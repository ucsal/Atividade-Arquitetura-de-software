spring:
  application:
    name: config-server
  cloud:
    config:
      server:
        git:
          uri: file:///home/ubuntu/spring_cloud_config_project/config-repo
          default-label: master
          search-paths: /
  security:
    user:
      name: admin
      password: admin123

server:
  port: 8888

management:
  endpoints:
    web:
      exposure:
        include: "*"
  endpoint:
    health:
      show-details: always
